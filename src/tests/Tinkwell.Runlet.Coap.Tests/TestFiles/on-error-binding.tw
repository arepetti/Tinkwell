coap sensors {
    port = 5683

    resource "/sensor/+" {
        on post {
            bind measure from "Tinkwell.Integrations" {
                name = (segment(path, -1))
                on error stop this;
            }
        }
    }
}
